@model IEnumerable<ControleContentoresV2.Models.situacao>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.situacao1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.situacao1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idSituacao }) |
            @Html.ActionLink("Details", "Details", new { id=item.idSituacao }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idSituacao })
        </td>
    </tr>
}

</table>
